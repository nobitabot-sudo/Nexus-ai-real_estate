{
  "buildCommand": "pnpm --filter @workspace/re-dashboard run build",
  "outputDirectory": "artifacts/re-dashboard/dist/public",
  "installCommand": "pnpm install --frozen-lockfile",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
